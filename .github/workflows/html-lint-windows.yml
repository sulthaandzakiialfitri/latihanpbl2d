name: Cek Kode HTM: (Windows)

on:
push:
branches:
-main
pull request

jobs:
lint:
runs-on: windows-latest
steps:
-name: Checkout Repository
uses: actions/checkout@v4

-name: Install HTMLHInt
run: npm install -g htmlhint

-name: Periksa HTML
run: htmlhint "**/*.html"
